名字 = "yycchat" # 改成和CI一致的 yycchat
main = "src/chat.mjs"
compatibility_date = "2025-01-01"

# 保留原有配置，只改 name 即可
[[durable_objects.bindings]]
名字 = "ChatRoom"
class_name = "ChatRoom"

[[durable_objects.bindings]]
名字 = "RateLimiter"
class_name = "RateLimiter"

[assets]
directory = "./"
